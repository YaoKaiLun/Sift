# Sift 计划三 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修好转分隔线命中，并加上键盘切文件、后缀过滤、未推送 commit 阅读、GitHub Release 检查更新；每一刀结束应用都能用。

**Architecture:** 分栏热区提到 `ContentView` 的 overlay，不再靠 1pt 父框溢出命中。过滤是 `DiffEngine.FileFilter` 纯函数，中栏与连续滚动共用可见列表。未推送走 `GitKit` 的 `@{upstream}..HEAD` name-status / diff，`DiffCacheKey` 用 `DiffSide` 和工作区缓存隔离。更新是新建的 `UpdateKit`（Foundation-only），UI 不进 `RepoStore`。

**Tech Stack:** Swift 6、SwiftPM、SwiftUI 外壳、AppKit `NSEvent` monitor / `NSTextView`、FSEvents、`URLSession`、`hdiutil`、XCTest。零第三方依赖。

**规格：** `docs/superpowers/specs/2026-09-14-sift-phase-3-design.md`

## Global Constraints

- 最低系统版本：macOS 26。
- 零第三方依赖。不引入 Sparkle。
- **主线程上永不调用 git，也不发网络请求。**
- **永不轮询 git。** 更新检查只在启动（Release）和菜单「检查更新」时发生。
- **切换即取消读任务。**
- 所有操作鼠标可达。方向键只是加速。
- 性能门禁数字不放宽。`./Scripts/preflight.sh` 必须继续通过。
- 注释与提交说明用中文。
- 未跟踪文件 discard 仍是全应用唯一确认框。未推送视图禁止 stage / discard。

---

## File Structure

```
Sources/
  GitKit/
    NameStatusParser.swift         新建：git diff --name-status -z
    FileDiff.swift                 BlobSource 增加 revision(String)
    GitRepository.swift            unpushedSummary / diffUnpushed / readBlob.revision
  DiffEngine/
    FileFilter.swift               新建：glob 隐藏规则
    DiffCache.swift                DiffCacheKey.side: DiffSide
    DiffEngine.swift               load 接受 DiffSide；未推送 / 图片 revision
  RepoStore/
    PersistedState.swift           fileListSource、hidesFilteredFiles、fileFilterPatterns
    RepoStore.swift                可见列表、未推送刷新、键盘用的有序列表
  UpdateKit/                       新建
    Version.swift
    GitHubLatestRelease.swift
    UpdateChecker.swift
    UpdateInstaller.swift
  SiftUI/
    SplitDivider.swift             11pt 容器、居中 1pt 线
    SplitOverlayLayout.swift       新建：热区 minX 纯函数
    ContentView.swift              overlay 分栏线；更新条；键盘 monitor 安装
    FileListPane.swift             过滤、未推送分组、无复选框、键盘
    SourceSidebar.swift            页脚「更新」按钮
    DiffPane.swift                 未推送时 hunkActions = nil
    FileFilterEditor.swift         新建：规则 popover
    UpdateBanner.swift             新建：就绪条
    SettingsView.swift             不动（过滤不进模型配置）
  App/Sift/SiftApp.swift           检查更新菜单；启动检查
Tests/
  GitKitTests/NameStatusParserTests.swift
  GitKitTests/UnpushedTests.swift
  DiffEngineTests/FileFilterTests.swift
  DiffEngineTests/DiffCacheTests.swift   增补 side 隔离
  DiffEngineTests/SplitOverlayLayoutTests.swift
  RepoStoreTests/PersistedStateTests.swift  增补缺省
  RepoStoreTests/FileFilterStoreTests.swift
  UpdateKitTests/VersionTests.swift
  UpdateKitTests/GitHubLatestReleaseTests.swift
```

实施顺序：A → C 的纯函数与持久化 → B → C 的 UI → D → E。键盘依赖「可见文件」列表，过滤纯函数要先落地，UI 可以和键盘同一批接上。

---

### Task 1: 分栏热区坐标纯函数

**Files:**
- Create: `Sources/SiftUI/SplitOverlayLayout.swift`
- Create: `Tests/DiffEngineTests/SplitOverlayLayoutTests.swift`（现有 DiffEngineTests 已依赖 SiftUI，沿用）

**Interfaces:**
- Produces: `SplitOverlayLayout.hitMinXs(showsSidebar:sidebarWidth:fileListWidth:hitWidth:) -> (sidebar: CGFloat?, fileList: CGFloat)`。热区以分界线为中心，`minX = boundary - hitWidth/2`。

- [ ] **Step 1: 写失败的测试**

```swift
import XCTest
@testable import SiftUI

final class SplitOverlayLayoutTests: XCTestCase {
    func testHitMinXsCentersOnBoundaries() {
        let hits = SplitOverlayLayout.hitMinXs(
            showsSidebar: true, sidebarWidth: 220, fileListWidth: 300, hitWidth: 11)
        XCTAssertEqual(hits.sidebar, 220 - 5.5)
        XCTAssertEqual(hits.fileList, 220 + 300 - 5.5)
    }

    func testHiddenSidebarOmitsFirstDivider() {
        let hits = SplitOverlayLayout.hitMinXs(
            showsSidebar: false, sidebarWidth: 220, fileListWidth: 300, hitWidth: 11)
        XCTAssertNil(hits.sidebar)
        XCTAssertEqual(hits.fileList, 300 - 5.5)
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter SplitOverlayLayoutTests`
Expected: FAIL，`SplitOverlayLayout` 不存在。

- [ ] **Step 3: 最小实现**

```swift
import CoreGraphics

enum SplitOverlayLayout {
    static func hitMinXs(showsSidebar: Bool, sidebarWidth: CGFloat,
                         fileListWidth: CGFloat, hitWidth: CGFloat = 11)
        -> (sidebar: CGFloat?, fileList: CGFloat) {
        let half = hitWidth / 2
        let fileListBoundary = (showsSidebar ? sidebarWidth : 0) + fileListWidth
        return (
            showsSidebar ? sidebarWidth - half : nil,
            fileListBoundary - half
        )
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter SplitOverlayLayoutTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/SiftUI/SplitOverlayLayout.swift Tests/DiffEngineTests/SplitOverlayLayoutTests.swift
git commit -m "test: 分栏热区坐标以边界为中心"
```

---

### Task 2: 把分隔线提到 ContentView overlay

**Files:**
- Modify: `Sources/SiftUI/SplitDivider.swift`
- Modify: `Sources/SiftUI/ContentView.swift`

**Interfaces:**
- Consumes: `SplitOverlayLayout.hitMinXs`
- Produces: `SplitDivider` 自身布局宽度改为 `hitWidth`（默认 11），内部仍画 1pt 线。`ContentView` 三栏 `.clipped()`，分隔线不再插在 HStack 子视图中间。

- [ ] **Step 1: 改 `SplitDivider` 为参与布局的 11pt 热区**

把根视图改成 `Color.clear.frame(width: 11).overlay { 1pt 线 }.overlay { 3pt 活动态 }.contentShape(Rectangle())`，hover / drag / 光标逻辑保持不变。1pt 线水平居中。不要再把 11pt 画在 1pt 父框的 overlay 里。

- [ ] **Step 2: 改 `ContentView`**

```swift
HStack(spacing: 0) {
    if showsSidebar {
        SourceSidebar()
            .frame(width: store.sidebarWidth)
            .clipped()
    }
    FileListPane(showsSidebar: $showsSidebar)
        .frame(width: store.fileListWidth)
        .clipped()
    DiffPane()
        .frame(maxWidth: .infinity)
        .clipped()
}
.overlay(alignment: .leading) {
    let hits = SplitOverlayLayout.hitMinXs(
        showsSidebar: showsSidebar,
        sidebarWidth: store.sidebarWidth,
        fileListWidth: store.fileListWidth)
    ZStack(alignment: .leading) {
        if let x = hits.sidebar {
            SplitDivider(width: sidebarWidthBinding, range: 180...340,
                         onDragEnded: { store.persist() })
                .offset(x: x)
        }
        SplitDivider(width: fileListWidthBinding, range: 240...520,
                     onDragEnded: { store.persist() })
            .offset(x: hits.fileList)
    }
    .allowsHitTesting(true)
}
```

`ZStack` 里不要铺满 `Color.clear`（会抢走三栏点击）。`SplitDivider` 现在宽 11pt，`offset` 用 `hitMinXs` 的 minX。

- [ ] **Step 3: 跑热区测试 + 编译 SiftUI**

Run: `swift test --filter SplitOverlayLayoutTests`
Expected: PASS。本地再用有一屏文件的仓库确认分隔线全高可拖（Cloud Agent 无 macOS GUI 时在 PR 说明里写明需真人点一次）。

- [ ] **Step 4: Commit**

```bash
git add Sources/SiftUI/SplitDivider.swift Sources/SiftUI/ContentView.swift
git commit -m "fix: 分栏线热区提到窗口 overlay，文件行不再挡住拖动"
```

---

### Task 3: FileFilter 纯函数

**Files:**
- Create: `Sources/DiffEngine/FileFilter.swift`
- Create: `Tests/DiffEngineTests/FileFilterTests.swift`

**Interfaces:**
- Produces:

```swift
public enum FileFilter: Sendable {
    public static let defaultPatterns: [String]
    public static func matches(path: String, patterns: [String]) -> Bool
    public static func hiding<T>(_ items: [T], path: (T) -> String,
                                 enabled: Bool, patterns: [String]) -> [T]
}
```

匹配语义与 `GeneratedFileDetector` 相同：`foo/` 匹配路径段，否则 `fnmatch` 文件名。空规则或 `enabled == false` 时 `hiding` 返回原数组。

- [ ] **Step 1: 写失败的测试**

```swift
import XCTest
@testable import DiffEngine

final class FileFilterTests: XCTestCase {
    func testImageSuffix() {
        XCTAssertTrue(FileFilter.matches(path: "docs/shot.PNG", patterns: ["*.png"]))
        XCTAssertFalse(FileFilter.matches(path: "src/App.ts", patterns: ["*.png"]))
    }

    func testTestFileGlobs() {
        XCTAssertTrue(FileFilter.matches(path: "FooTest.swift", patterns: ["*Test.swift"]))
        XCTAssertTrue(FileFilter.matches(path: "a.spec.ts", patterns: ["*.spec.ts"]))
        XCTAssertFalse(FileFilter.matches(path: "Foo.swift", patterns: ["*Test.swift"]))
    }

    func testDirectorySegment() {
        XCTAssertTrue(FileFilter.matches(path: "src/fixtures/a.json", patterns: ["fixtures/"]))
        XCTAssertFalse(FileFilter.matches(path: "src/fixture-data/a.json", patterns: ["fixtures/"]))
    }

    func testHidingDisabledKeepsAll() {
        let paths = ["a.png", "b.ts"]
        XCTAssertEqual(
            FileFilter.hiding(paths, path: { $0 }, enabled: false, patterns: ["*.png"]),
            paths)
    }

    func testHidingDropsMatches() {
        XCTAssertEqual(
            FileFilter.hiding(["a.png", "b.ts"], path: { $0 },
                              enabled: true, patterns: ["*.png"]),
            ["b.ts"])
    }
}
```

`*.png` 对 `shot.PNG`：`fnmatch` 默认区分大小写。规格要求后缀过滤好用，实现里对**规则和文件名都转小写再匹配**（目录段同样）。测试按小写不敏感来写。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter FileFilterTests`
Expected: FAIL

- [ ] **Step 3: 实现**

把 `GeneratedFileDetector.matchesGlob` 的 `fnmatch` 抽成 `FileFilter` 内部函数，或在 `FileFilter` 里复制一份（detector 继续自用，避免一次改两处语义）。`defaultPatterns` 按规格第 5.1 节原样列出。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter FileFilterTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/DiffEngine/FileFilter.swift Tests/DiffEngineTests/FileFilterTests.swift
git commit -m "feat: 按 glob 判断文件是否应被过滤隐藏"
```

---

### Task 4: 持久化过滤与数据源字段

**Files:**
- Modify: `Sources/RepoStore/PersistedState.swift`
- Modify: `Sources/RepoStore/RepoStore.swift`（属性 + `persist()`）
- Modify: `Tests/RepoStoreTests/PersistedStateTests.swift`

**Interfaces:**
- Produces:

```swift
public enum FileListSource: String, Codable, Sendable {
    case workingTree
    case unpushed
}

public struct PersistedState {
    public var fileListSource: FileListSource
    public var hidesFilteredFiles: Bool
    public var fileFilterPatterns: [String]
}
```

缺省：`.workingTree`、`false`、`FileFilter.defaultPatterns`。`decodeIfPresent` 缺失时用缺省。`RepoStore` 增加对应 `@Observable` 属性，`didSet` 里 `persist()`（栏宽那种拖动中的字段除外；这三个都是离散变更，直接 persist）。

- [ ] **Step 1: 写失败的测试**

在 `PersistedStateTests` 增加：

```swift
func testMissingFilterFieldsUseDefaults() throws {
    let url = temporaryFile()
    defer { try? FileManager.default.removeItem(at: url.deletingLastPathComponent()) }
    try FileManager.default.createDirectory(
        at: url.deletingLastPathComponent(), withIntermediateDirectories: true)
    try Data(#"{"repositoryBookmarks":[],"usesTreeView":true}"#.utf8).write(to: url)

    let state = PersistedStateStore(fileURL: url).load()
    XCTAssertEqual(state.fileListSource, .workingTree)
    XCTAssertFalse(state.hidesFilteredFiles)
    XCTAssertEqual(state.fileFilterPatterns, FileFilter.defaultPatterns)
}
```

`PersistedState` 在 RepoStore，测试已 `import RepoStore`；`FileFilter` 在 DiffEngine，RepoStore 已依赖 DiffEngine，测试 target 需 `import DiffEngine` 或把 `defaultPatterns` 的期望写死一份字符串数组。优先 `import DiffEngine`。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter PersistedStateTests/testMissingFilterFieldsUseDefaults`
Expected: FAIL，字段不存在。

- [ ] **Step 3: 补字段并接到 RepoStore.init / persist()**

`persist()` 构造 `PersistedState` 时传入新三字段。init 从 `loaded` 读出。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter PersistedStateTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/RepoStore/PersistedState.swift Sources/RepoStore/RepoStore.swift Tests/RepoStoreTests/PersistedStateTests.swift
git commit -m "feat: 持久化文件过滤规则与中栏数据源"
```

---

### Task 5: 中栏应用过滤

**Files:**
- Modify: `Sources/RepoStore/RepoStore.swift`
- Modify: `Sources/SiftUI/FileListPane.swift`
- Create: `Sources/SiftUI/FileFilterEditor.swift`
- Create: `Tests/RepoStoreTests/FileFilterStoreTests.swift`

**Interfaces:**
- Consumes: `FileFilter.hiding`、`store.fileFilterPatterns`、`store.hidesFilteredFiles`
- Produces: `RepoStore.visibleFileStatuses: [FileStatus]`（对 `fileStatuses` 过滤）。`refreshFileList` 结束后若当前选中路径已被隐藏，走现有 `reconcileSelection` 清空逻辑。`rebuildContinuousPlanIfNeeded`（或等价路径）必须用可见列表。

- [ ] **Step 1: 写失败的测试**

用 `FixtureRepo` 造 `shot.png` + `a.txt` 两个未跟踪/未暂存文件，`RepoStore` 选中该 worktree 后：

```swift
store.fileFilterPatterns = ["*.png"]
store.hidesFilteredFiles = true
XCTAssertFalse(store.visibleFileStatuses.contains { $0.path.hasSuffix(".png") })
XCTAssertTrue(store.visibleFileStatuses.contains { $0.path == "a.txt" })
```

需要一个能 `addRepository` 的测试。仿 `RepoStoreTests` 现有 `addRepository` 用例。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter FileFilterStoreTests`
Expected: FAIL，`visibleFileStatuses` 不存在。

- [ ] **Step 3: 实现 store + UI**

`visibleFileStatuses`：

```swift
public var visibleFileStatuses: [FileStatus] {
    FileFilter.hiding(fileStatuses, path: \.path,
                      enabled: hidesFilteredFiles,
                      patterns: fileFilterPatterns)
}
```

`FileListPane.rows` 改用 `visibleFileStatuses`。分组计数用可见数。`hiddenFileCount = fileStatuses.count - visibleFileStatuses.count`（工作区同一路径可出现 staged+unstaged 两次，计数用 `fileStatuses` 过滤前后的差即可，不必去重）。

栏头 trailing 最左侧加隐藏开关 + 规则菜单。`FileFilterEditor`：`TextEditor` 绑定本地 `@State` 文本，`onDisappear` / 恢复默认按钮写回 `store.fileFilterPatterns`（按行 trim，丢掉空行）。

空状态：`selectedWorktree != nil && fileStatuses.isEmpty` 仍是「没有改动」；`fileStatuses` 非空且 `visibleFileStatuses` 空 → 「过滤后没有文件」。

连续滚动 plan 的输入改为 `visibleFileStatuses`。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter FileFilterStoreTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/RepoStore/RepoStore.swift Sources/SiftUI/FileListPane.swift Sources/SiftUI/FileFilterEditor.swift Tests/RepoStoreTests/FileFilterStoreTests.swift
git commit -m "feat: 中栏按规则一键隐藏图片和测试文件"
```

---

### Task 6: 上下键切换可见文件

**Files:**
- Modify: `Sources/RepoStore/RepoStore.swift`
- Modify: `Sources/SiftUI/FileListPane.swift`
- Modify: `Sources/SiftUI/ContentView.swift`
- Modify: `Tests/RepoStoreTests/RepoStoreTests.swift`（或新文件 `FileNavigationTests.swift`）

**Interfaces:**
- Produces:

```swift
extension RepoStore {
    public func selectAdjacentFile(delta: Int, extending: Bool, orderedIDs: [String]) async
}
```

`delta` 为 +1 / -1。`orderedIDs` 由 UI 传入（树折叠只含可见行，store 不知道目录展开态）。无选中：+1 取 first，-1 取 last。找不到当前 id 时与无选中相同。`extending == true` 调现有 `selectFileRange`。

- [ ] **Step 1: 写失败的测试**

```swift
await store.select(file: files[0], staged: false)
await store.selectAdjacentFile(delta: 1, extending: false, orderedIDs: ids)
XCTAssertEqual(store.selectedFile?.path, files[1].path)

await store.selectAdjacentFile(delta: 1, extending: false, orderedIDs: ids)
XCTAssertEqual(store.selectedFile?.path, files[2].path)

await store.selectAdjacentFile(delta: 1, extending: false, orderedIDs: ids)
XCTAssertEqual(store.selectedFile?.path, files[2].path) // 端点停住
```

至少 3 个工作区文件。`ids` 用 `fileSelectionID` 按列表顺序构造。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter selectAdjacentFile`
Expected: FAIL

- [ ] **Step 3: 实现 store 方法 + UI monitor**

`FileListPane` 把 `orderedFileIDs` 通过回调或 `store.fileListOrderedIDs` 同步出去。更干净的做法：`ContentView` 不持有列表，在 `FileListPane.onAppear` 里：

```swift
.background(
    FileListKeyMonitor(orderedIDs: orderedFileIDs) { delta, extending in
        Task { await store.selectAdjacentFile(delta: delta, extending: extending,
                                              orderedIDs: orderedFileIDs) }
    }
)
```

`FileListKeyMonitor` 是 `NSViewRepresentable`，`viewDidMoveToWindow` 时 `NSEvent.addLocalMonitorForEvents(matching: .keyDown)`。只处理 `keyCode` 125/126（↓/↑）。若 `NSApp.keyWindow?.firstResponder` 是 `NSTextField` / `NSSecureTextField`，或是 `NSTextView` 但不是 `DiffCopyTextView`（定义在 `DiffTextView.swift`），返回原 event。`DiffCopyTextView` 上的方向键要切文件，不要移动插入点。`NSEvent.modifierFlags` 含 `.shift` 则 `extending: true`；含 `.command` / `.option` 则放行。`onDisappear` `removeMonitor`。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter FileNavigation`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/RepoStore/RepoStore.swift Sources/SiftUI/FileListPane.swift Sources/SiftUI/ContentView.swift Tests/RepoStoreTests/FileNavigationTests.swift
git commit -m "feat: 上下方向键切换中栏可见文件"
```

---

### Task 7: NameStatusParser

**Files:**
- Create: `Sources/GitKit/NameStatusParser.swift`
- Create: `Tests/GitKitTests/NameStatusParserTests.swift`

**Interfaces:**
- Produces: `NameStatusParser.parse(_ data: Data) throws -> [FileStatus]`
- 状态字母：`M` modified、`A` added、`D` deleted、`T` typeChanged、`U` unmerged；`R*` / `C*` 为 renamed/copied，其后两个 NUL 字段是 old/new path。`indexStatus` 为该种类，`worktreeStatus` 为 `.unmodified`。

- [ ] **Step 1: 写失败的测试**

手工拼 `-z` 字节：`M\0a.txt\0A\0b.txt\0D\0c.txt\0R100\0old.txt\0new.txt\0`。断言四条 `FileStatus` 的 path / originalPath / indexStatus。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter NameStatusParserTests`
Expected: FAIL

- [ ] **Step 3: 实现解析器**

按 NUL 切分，步进消费。未知字母跳过（与 StatusParser 对未来类型的策略一致），不要整表失败。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter NameStatusParserTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/GitKit/NameStatusParser.swift Tests/GitKitTests/NameStatusParserTests.swift
git commit -m "feat: 解析 git diff --name-status -z"
```

---

### Task 8: GitRepository 未推送 API

**Files:**
- Modify: `Sources/GitKit/GitRepository.swift`
- Modify: `Sources/GitKit/FileDiff.swift`（`BlobSource.revision(String)`）
- Create: `Tests/GitKitTests/UnpushedTests.swift`
- Modify: `Tests/GitKitTests/FixtureRepo.swift` 不必改；clone 目录用 `Process` 调 git。

**Interfaces:**
- Produces: `UnpushedSummary`、`GitRepository.unpushedSummary() async throws -> UnpushedSummary?`、`diffUnpushed(path:upstream:) async throws -> FileDiff`。`readBlob` 对 `.revision(rev)` 走 `gitBlob(spec: "\(rev):\(path)")`。

无上游：`rev-parse --abbrev-ref --symbolic-full-name @{upstream}` 非零 → 返回 `nil`，不抛。

- [ ] **Step 1: 写失败的测试**

```swift
func testUnpushedSummaryNilWithoutUpstream() async throws {
    let fixture = try FixtureRepo()
    try fixture.write("a\n", to: "a.txt")
    try fixture.commit("initial")
    let summary = try await GitRepository(root: fixture.url).unpushedSummary()
    XCTAssertNil(summary)
}

func testUnpushedSummaryListsCommittedFiles() async throws {
    let origin = try FixtureRepo()
    try origin.write("a\n", to: "a.txt")
    try origin.commit("initial")
    let cloneURL = URL(fileURLWithPath: NSTemporaryDirectory())
        .appendingPathComponent("sift-unpushed-\(UUID().uuidString)")
    try origin.git("clone", origin.url.path, cloneURL.path)
    defer { try? FileManager.default.removeItem(at: cloneURL) }
    let clone = GitRepository(root: cloneURL)
    // 在 clone 里提交新文件：用 FixtureRepo 同款 git 调用写盘后 add/commit。
    let b = cloneURL.appendingPathComponent("b.txt")
    try Data("new\n".utf8).write(to: b)
    let git: (String...) throws -> Void = { args in
        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/usr/bin/git")
        process.arguments = Array(args)
        process.currentDirectoryURL = cloneURL
        try process.run()
        process.waitUntilExit()
        XCTAssertEqual(process.terminationStatus, 0)
    }
    try git("config", "user.email", "test@sift.local")
    try git("config", "user.name", "Sift Test")
    try git("config", "commit.gpgsign", "false")
    try git("add", "b.txt")
    try git("commit", "-m", "add b")

    let summary = try await clone.unpushedSummary()
    XCTAssertEqual(summary?.aheadCount, 1)
    XCTAssertEqual(summary?.files.map(\.path), ["b.txt"])
}
```

搭建方式：两个 `FixtureRepo`，`git clone --bare` 或 `remote add` + `branch -u`。推荐：

1. repoA 初始 commit，`git clone` 到 repoB（clone 自带 origin/main）。
2. 在 repoB `write b.txt` + `commit`。
3. `unpushedSummary()` 于 repoB：ahead 1，文件 `b.txt`。

再测 `diffUnpushed`：b.txt 全文为新增。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter UnpushedTests`
Expected: FAIL

- [ ] **Step 3: 实现**

```swift
public func unpushedSummary() async throws -> UnpushedSummary? {
    let up = try await runner.runAllowingFailure(
        ["rev-parse", "--abbrev-ref", "--symbolic-full-name", "@{upstream}"], in: root)
    guard up.exitCode == 0 else { return nil }
    let upstream = String(decoding: up.stdout, as: UTF8.self)
        .trimmingCharacters(in: .whitespacesAndNewlines)
    let countData = try await runner.run(
        ["rev-list", "--count", "\(upstream)..HEAD"], in: root)
    let ahead = Int(String(decoding: countData, as: UTF8.self)
        .trimmingCharacters(in: .whitespacesAndNewlines)) ?? 0
    guard ahead > 0 else {
        return UnpushedSummary(upstream: upstream, aheadCount: 0, files: [], lineStats: [:])
    }
    async let nameData = runner.run(
        ["diff", "--name-status", "-z", "\(upstream)..HEAD"], in: root)
    async let numData = runner.run(
        ["diff", "--numstat", "-z", "\(upstream)..HEAD"], in: root)
    let files = try NameStatusParser.parse(try await nameData)
    let stats = NumstatParser.parse(try await numData)
    return UnpushedSummary(upstream: upstream, aheadCount: ahead,
                           files: files, lineStats: stats)
}

public func diffUnpushed(path: String, upstream: String) async throws -> FileDiff {
    let data = try await runner.run(
        ["diff", "--no-color", "-U3", "\(upstream)..HEAD", "--", path], in: root)
    return DiffParser.parse(data, path: path)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter UnpushedTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/GitKit Tests/GitKitTests
git commit -m "feat: 读取相对上游未推送提交的文件列表与 diff"
```

---

### Task 9: DiffSide 隔离缓存并加载未推送 diff

**Files:**
- Modify: `Sources/DiffEngine/DiffCache.swift`
- Modify: `Sources/DiffEngine/DiffEngine.swift`
- Modify: `Tests/DiffEngineTests/DiffCacheTests.swift`
- Modify: 所有构造 `DiffCacheKey(..., staged:)` 的调用点（`DiffEngine` 内部为主）

**Interfaces:**
- Produces:

```swift
public enum DiffSide: Hashable, Sendable, Equatable {
    case workingTree(staged: Bool)
    case unpushed
}

public struct DiffCacheKey: Sendable, Hashable {
    public let worktreePath: URL
    public let filePath: String
    public let side: DiffSide
}
```

`DiffEngine.load(status:side:from:)` 替换 `staged: Bool`。工作区分支保持原样（`repository.diff(path:staged:)`）。`.unpushed` 调 `repository.diffUnpushed`；图片 `blobRefs` 旧侧 `.revision(upstream)`、新侧 `.revision("HEAD")`。`load` 需要上游名：给 `GitRepository` 在 summary 之后由 store 传入，或 `load` 增加 `upstream: String?`。采用：

```swift
public func load(status: FileStatus, side: DiffSide, from repository: GitRepository,
                 upstream: String? = nil, ignoringCollapse: Bool = false) async throws -> LoadedDiff
```

未推送且 `upstream == nil` 视为编程错误，返回 `.ready` empty 即可，不要崩。

- [ ] **Step 1: 写失败的测试**

`DiffCacheTests`：同一 path 插入 `.workingTree(staged: false)` 与 `.unpushed` 两条，断言 `value(for:)` 互不覆盖。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter DiffCacheTests`
Expected: FAIL（key 仍是 staged Bool，或测试尚未编译）

- [ ] **Step 3: 改 key 与 load**

工作区调用改为 `side: .workingTree(staged:)`。`RepoStore` 所有 `engine.load(status:staged:)` 同步改签名。图片 `blobRefs` 增加 unpushed 分支。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter DiffCacheTests`
Expected: PASS。全量 `swift test --filter DiffEngineTests` 也要绿。

- [ ] **Step 5: Commit**

```bash
git add Sources/DiffEngine Sources/RepoStore Tests/DiffEngineTests
git commit -m "feat: Diff 缓存按工作区与未推送隔离"
```

---

### Task 10: RepoStore 未推送数据源

**Files:**
- Modify: `Sources/RepoStore/RepoStore.swift`
- Create: `Tests/RepoStoreTests/UnpushedStoreTests.swift`

**Interfaces:**
- Consumes: `GitRepository.unpushedSummary`、`fileListSource`
- Produces: `unpushedUpstream: String?`、`unpushedAheadCount: Int`、`unpushedUnavailableReason: enum { none, noUpstream, noneAhead }`（或用 optional summary）。`refreshFileList()` 按 `fileListSource` 分支：工作区走现有 `status()` + `lineStats()`；未推送走 `unpushedSummary()`，把 `files` 写入 `fileStatuses`，`lineStats` 写入 `stagedLineStats` 且 `unstagedLineStats = [:]`（中栏只读一组数字）。

切换 `fileListSource`：取消读任务、清空选中与 diff、`refreshFileList()`。

`select(file:)`：若 `fileListSource == .unpushed`，`engine.load(..., side: .unpushed, upstream: unpushedUpstream)`。`selectedFileIsStaged` 在未推送模式保持 `false`。`fileSelectionID` 未推送用 `"p:" + path`，避免和工作区 `u:` 撞。`isFileSelected` / 点击路径同步改。

写操作（stage/unstage/discard）在 `fileListSource == .unpushed` 时直接 `return`。

- [ ] **Step 1: 写失败的测试**

Clone fixture 如 Task 8。`addRepository` + `select` worktree，`fileListSource = .unpushed`，等待 refresh：

```swift
XCTAssertEqual(store.unpushedAheadCount, 1)
XCTAssertEqual(store.fileStatuses.map(\.path), ["b.txt"])
await store.select(file: store.fileStatuses[0], staged: false)
XCTAssertNotNil(store.loadedDiff)
```

无 upstream 的 repo：`fileStatuses` 空，`unpushedUnavailableReason == .noUpstream`。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter UnpushedStoreTests`
Expected: FAIL

- [ ] **Step 3: 实现 refresh 分支与 select 分支**

`visibleFileStatuses` 仍然包一层 FileFilter。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter UnpushedStoreTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/RepoStore Tests/RepoStoreTests
git commit -m "feat: 中栏可切换为未推送提交的文件列表"
```

---

### Task 11: 未推送 UI

**Files:**
- Modify: `Sources/SiftUI/FileListPane.swift`
- Modify: `Sources/SiftUI/DiffPane.swift`
- Modify: `Sources/SiftUI/PaneHeader.swift` 仅当 trailing 放不下时把数据源 toggle 放进 title 旁；优先塞进现有 trailing 最左。

**Interfaces:**
- Consumes: `store.fileListSource`、`unpushedAheadCount`、`unpushedUnavailableReason`
- Produces: 未推送时单一分组「未推送」，无 `Toggle` 复选框；空状态文案按规格 6.1；`hunkActions` 在未推送时返回 `nil`。栏头 subtitle：工作区仍是 worktree 名；未推送为「领先 N 个提交」或无上游时不显示数字。

`PlainIconToggle` 目前是 `Binding<Bool>`。数据源是枚举。不要硬拧 Bool。新增：

```swift
struct FileListSourceToggle: View {
    @Binding var source: FileListSource
}
```

两个 `PlainIconButton`，`doc.text` / `arrow.up.circle`，`isSelected` 按枚举。help：「查看工作区改动」/「查看未推送的提交」。

连续滚动：plan 已用 `visibleFileStatuses`；未推送时 `ContinuousDiffEntry` 的 id 用 `p:path`，`expand` 走 `.unpushed`。检查 `Sources/DiffEngine/ContinuousDiffPlan.swift` 与 `RepoStore` 里构造 plan 的地方，把 `staged` 换成 side。

- [ ] **Step 1: 改 FileListPane 分组**

```swift
if store.fileListSource == .unpushed {
    append(&result, title: "未推送",
           statuses: store.visibleFileStatuses.sorted(by: FileStatus.pathOrder),
           staged: false, showsCheckbox: false)
} else {
    // 现有已暂存 / 未暂存
}
```

`fileRow` 增加 `showsCheckbox`；false 时用 `Color.clear.frame(width: Theme.checkboxColumnWidth)` 占位，避免和树视图错位——或直接不占位让路径左移。规格：没有复选框。不占位，路径与状态徽章左对齐到原来复选框位置。

- [ ] **Step 2: DiffPane hunkActions**

```swift
if store.fileListSource == .unpushed { return nil }
```

连续模式同样判断。

- [ ] **Step 3: 空状态**

无上游 / 领先 0 / 过滤后为空，三路文案按规格，不要共用「没有改动」。

- [ ] **Step 4: 编译与 GitKit/RepoStore 测试**

Run: `swift test --filter Unpushed`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/SiftUI Sources/RepoStore Sources/DiffEngine
git commit -m "feat: 未推送视图隐藏暂存操作并展示领先提交数"
```

---

### Task 12: UpdateKit 版本与 Release JSON

**Files:**
- Modify: `Package.swift` 增加 `UpdateKit` library + `UpdateKitTests`
- Create: `Sources/UpdateKit/Version.swift`
- Create: `Sources/UpdateKit/GitHubLatestRelease.swift`
- Create: `Tests/UpdateKitTests/VersionTests.swift`
- Create: `Tests/UpdateKitTests/GitHubLatestReleaseTests.swift`

**Interfaces:**
- Produces:

```swift
public struct Version: Sendable, Equatable, Comparable {
    public init?(_ string: String)  // 去掉 v 前缀；非数字段失败返回 nil
    public static func < (lhs: Version, rhs: Version) -> Bool
}

public struct GitHubLatestRelease: Sendable, Equatable {
    public var tagName: String
    public var version: Version? { Version(tagName) }
    public var dmgURL: URL?
    public static func decode(_ data: Data) throws -> GitHubLatestRelease
}
```

只认 `assets` 里 `name` 匹配 `^Sift-.*\\.dmg$` 的第一项 `browser_download_url`。

- [ ] **Step 1: 写失败的测试**

版本：`1.0 < 1.0.1 < 1.2 < 1.10`；`v1.2.3` 可解析；`1.0 == 1.0.0`。

JSON fixture：含 `tag_name: v1.1` 与 `Sift-1.1.dmg` 资产，断言 `dmgURL`。缺 dmg 时 `dmgURL == nil`。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter UpdateKitTests`
Expected: FAIL（target 尚未加也可，先加空 target 再写测试）

- [ ] **Step 3: 实现 Version 与 decode**

Comparable：逐段比 `Int`，缺段当 0。不要用字符串比较（`1.10` 否则会小于 `1.2`）。

`GitHubLatestRelease` 用 `JSONDecoder` + `CodingKeys`：`tagName = tag_name`，`assets: [{name, browser_download_url}]`。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter UpdateKitTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Package.swift Sources/UpdateKit Tests/UpdateKitTests
git commit -m "feat: 解析 GitHub Release 版本与 DMG 地址"
```

---

### Task 13: 检查与安装器（可注入）

**Files:**
- Create: `Sources/UpdateKit/UpdateChecker.swift`
- Create: `Sources/UpdateKit/UpdateInstaller.swift`
- Create: `Tests/UpdateKitTests/UpdateCheckerTests.swift`
- Create: `Tests/UpdateKitTests/UpdateInstallerTests.swift`

**Interfaces:**
- Produces:

```swift
public struct AvailableUpdate: Sendable, Equatable {
    public var version: Version
    public var dmgURL: URL
}

public protocol ReleaseFetching: Sendable {
    func latestReleaseData() async throws -> Data
}

public struct GitHubReleaseFetcher: ReleaseFetching {
    public init(session: URLSession = .shared,
                url: URL = URL(string: "https://api.github.com/repos/YaoKaiLun/Sift/releases/latest")!)
}

public enum UpdateCheckResult: Sendable, Equatable {
    case upToDate
    case available(AvailableUpdate)
    case noInstallableAsset
}

public enum UpdateChecker {
    public static func check(current: Version, fetching: any ReleaseFetching) async throws -> UpdateCheckResult
}

public enum UpdateInstaller {
    public static func restartScript(pid: Int32, stagedApp: URL, targetApp: URL) -> String
}
```

Checker：decode → 无 version 或无 dmg → `.noInstallableAsset`；`remote <= current` → `.upToDate`；否则 `.available`。

`restartScript` 必须包含 `while kill -0 PID`、`ditto`、`xattr -cr`、`open`。

- [ ] **Step 1: 写失败的测试**

`ReleaseFetching` 假实现返回 Task 12 的 fixture。`current = 1.0` → available 1.1；`current = 1.1` → upToDate。

脚本：`restartScript(pid: 42, stagedApp: URL(fileURLWithPath: "/tmp/Sift-next.app"), targetApp: URL(fileURLWithPath: "/Applications/Sift.app"))` 必须含 `42`、`/tmp/Sift-next.app`、`/Applications/Sift.app`、`ditto`、`xattr -cr`、`open`。

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter UpdateCheckerTests`
Expected: FAIL

- [ ] **Step 3: 实现 fetcher / checker / script**

`GitHubReleaseFetcher` 用 `URLSession.data(from:)`，设置 `User-Agent`。不要在测试里打网；测试只走假 fetcher。

本 task 只交付：假 fetcher 的 `UpdateChecker.check`，以及 `restartScript` 纯函数。下载 DMG、`hdiutil attach`、把 `Sift.app` `ditto` 到 cache 的 `stageDownloadedApp(from:cacheDirectory:)` 在 Task 14 实现；CI 不挂载真实 DMG。

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter UpdateKitTests`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add Sources/UpdateKit Tests/UpdateKitTests
git commit -m "feat: 对照当前版本判断是否有可安装的 GitHub 更新"
```

---

### Task 14: 更新 UI 与菜单

**Files:**
- Create: `Sources/SiftUI/UpdateController.swift`（`@MainActor @Observable`）
- Create: `Sources/SiftUI/UpdateBanner.swift`
- Modify: `Sources/SiftUI/ContentView.swift`
- Modify: `Sources/SiftUI/SourceSidebar.swift`
- Modify: `App/Sift/SiftApp.swift`
- Modify: `Package.swift`：`SiftUI` 依赖 `UpdateKit`

**Interfaces:**
- Produces: `UpdateController.state`：`idle / checking / available(AvailableUpdate) / downloading / ready(version:staged:) / failed(String)`。方法：`check(automatic:)`、`download()`、`restart()`、`cancelDownload()`。

`SiftApp`：`@State private var updates = UpdateController()`，`.environment(updates)`。`WindowGroup` `.task { await updates.check(automatic: true) }`。Debug：`automatic: true` 时 `check` 立即 return。菜单：

```swift
CommandGroup(after: .appInfo) {
    Button("检查更新…") {
        Task { await updates.check(automatic: false) }
    }
}
```

手动检查 `upToDate`：`NSAlert` 或复用 `RepoStore.errorMessage` 不合适（那是 git 错）。用 `UpdateController.userMessage: String?` 在 `ContentView` 再一个 `.alert`。

侧栏页脚：`available` / `downloading` / `ready` 时在齿轮左侧放胶囊按钮。就绪时 `ContentView` 底部或顶边叠 `UpdateBanner`，点击 `restart()`。

`restart()`：写脚本到 `NSTemporaryDirectory()/sift-update.sh`，`Process` 跑 `/bin/bash`，然后 `NSApp.terminate(nil)`。

- [ ] **Step 1: UpdateController 可测部分**

能注入 `ReleaseFetching`。测试：假 release 高于当前 → `state` 变成 available（`Bundle` 版本在测试里用入参 `current: Version("1.0")!`，不要读真 bundle）。

把 `currentVersion` 做成 `UpdateController` 的 init 参数，App 传入从 bundle 读到的值；测试传入 `1.0`。

- [ ] **Step 2: 接 UI 与安装器**

实现 `UpdateInstaller.stageDownloadedApp(from:cacheDirectory:)`：`URLSession.download` 把 DMG 存进 cache → `hdiutil attach -nobrowse -readonly` → 在挂载卷上找 `Sift.app` → `ditto` 到 `cache/Sift-next.app` → `hdiutil detach`。全过程 `Task.detached`。CI 不调用此函数。

`restart()`：写脚本到 `NSTemporaryDirectory()/sift-update.sh`，`Process` 跑 `/bin/bash`，然后 `NSApp.terminate(nil)`。

文案：「更新」「下载中…」「重启」；条：「\(version) 已就绪 — 点击重启」。按钮视觉用现有 `FilledActionButtonStyle` 太重，侧栏用与 Cursor 类似的浅色胶囊：高度 22、圆角 11、`Color.accentColor.opacity(0.85)` 白字，或 `PlainIconButton` 加文字。优先小文字按钮，不要新图标集。

- [ ] **Step 3: README**

「使用」里加一句：Sift 菜单可检查更新；有新版本时侧栏出现「更新」。下载节保持 GitHub Release 手动安装，作为无自动更新时的路径。

- [ ] **Step 4: 跑 preflight**

Run: `./Scripts/preflight.sh`
Expected: 退出码 0

- [ ] **Step 5: Commit**

```bash
git add Sources/SiftUI App/Sift/SiftApp.swift Package.swift README.md Tests
git commit -m "feat: 检查 GitHub Release 并提示重启安装更新"
```

---

## 自检

| 规格条目 | 任务 |
|----------|------|
| A 分栏线全高可拖 | Task 1–2 |
| B 上下键切文件、输入框不拦截 | Task 6 |
| C glob 过滤 + 开关 + 编辑 + 连续滚动一致 | Task 3–5 |
| D 未推送 `@{u}..HEAD`、无 push、无 stage | Task 7–11 |
| E GitHub 检查、下载、就绪条、重启替换、零 Sparkle | Task 12–14 |
| 旧 state.json 缺省 | Task 4 |
| Diff 缓存不串味 | Task 9 |
| preflight | Task 14 |

无 TBD。`DiffEngine.load` 的 `staged: Bool` 在 Task 9 改为 `DiffSide`，Task 10 的 store 调用点必须用新签名——不要留旧重载以免工作区误走未推送。
